# Copy to .env (gitignored) and fill in real values. Never commit .env.
# Must match the API_KEY set in backend/.env — the frontend sends this as
# the x-api-key header on every request to the backend.
REACT_APP_API_KEY=
